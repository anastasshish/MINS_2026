// Иерархия исключений информационной системы аптеки

класс ошибка_аптеки(исключение) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_данных_аптеки(ошибка_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_формата_данных(ошибка_данных_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_не_найдено(ошибка_данных_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_недостаточно_на_складе(ошибка_данных_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_валидации(ошибка_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_формата(ошибка_валидации) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_склада(ошибка_валидации) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_рецепта(ошибка_валидации) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_срока_годности(ошибка_валидации) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}

класс ошибка_транзакции(ошибка_аптеки) {
    конструктор(строка сообщение) {
        этот.сообщение = сообщение;
    }
}
